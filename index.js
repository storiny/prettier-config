/** @type {import('prettier').Config} */
const config = {
    endOfLine: "auto",
    printWidth: 80,
    semi: true,
    singleQuote: false,
    tabWidth: 2,
    trailingComma: "none",
};

export default config;